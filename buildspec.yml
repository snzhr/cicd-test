version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build: 
    commands:
      - echo Installing npm dependencies
      - npm install
  build:
    commands:
      - echo Build start on 'date'
      - npm run build

artifacts:
  files: 
    - "**/*"
  discard:path: no
  base-directory: dist
